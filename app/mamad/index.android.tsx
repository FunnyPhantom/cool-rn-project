import { View, Text } from "react-native";

const Page = () => {
  return (
    <View style={{ backgroundColor: "white" }}>
      <Text>This is mamad page, but on ANDROID</Text>
    </View>
  );
};

export default Page;
